# Environment Setup

Before you can use the API, you need to configure your local environment variables.

## Step 1: Install the SDK

Run the following command in your terminal:

```bash
npm install @project/phoenix-sdk
```
<Button label="Click Me" />
